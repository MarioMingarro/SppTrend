% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spp_trend_environmental.R
\name{spp_trend_environmental}
\alias{spp_trend_environmental}
\title{Analyse Long-Term Environmental Niche Trends Across Species}
\usage{
spp_trend_environmental(
  data,
  spp = NULL,
  responses = c("Temperature", "Elevation"),
  min_records = 20,
  min_years = 5,
  alpha_ref = 0.05,
  use_Ncorrected_alpha = TRUE,
  individual_lm_min_thresholds = c(Temperature = 0.01, Elevation = 50),
  individual_lm_threshold_type = "speciesslope",
  verbose = TRUE
)
}
\arguments{
\item{data}{A data frame or \code{data.table} containing the raw observational
records.Must include the following columns (exact names required):
\describe{
\item{\code{Species}}{Character or factor.Taxonomic name or identifier for
each observation.}
\item{\code{Year}}{Integer or numeric.Calendar year of the observation.}
\item{\code{Month}}{Integer or numeric.Calendar month (1-12) of the
observation.}
\item{\code{Temperature}}{Numeric.Environmental temperature associated with
the observation (e.g. mean monthly temperature in deg C at the
sampling location).Required when \code{"Temperature"} is included in
\code{responses}.}
\item{\code{Elevation}}{Numeric.Elevation (m a.s.l.) associated with the
observation.Required when \code{"Elevation"} is included in
\code{responses}.}
}
Rows with \code{NA} in \code{Species}, \code{Year}, or \code{Month}, and rows where \code{Month}
is outside 1-12, are silently removed before analysis.}

\item{spp}{Character vector or \code{NULL} (default \code{NULL}).Names of the
species to include in the analysis.If \code{NULL}, all unique species present
in \code{data} are used.Species named in \code{spp} that are absent from \code{data}
trigger a warning; if none of the requested species are found the function
stops with an error.}

\item{responses}{Character vector specifying which environmental variables
to model.Allowed values are \code{"Temperature"}, \code{"Elevation"}, or both
(default \code{c("Temperature", "Elevation")}).The vector is matched against
the allowed set in the order \code{c("Temperature", "Elevation")}, so any
duplicate or out-of-order entries are normalised automatically.}

\item{min_records}{Positive integer (default \code{20}).Minimum number of
non-missing records a species must have for a given response variable to
be retained in the analysis.Species below this threshold are excluded
and reported in \code{species_filter}.}

\item{min_years}{Positive integer (default \code{5}).Minimum number of
distinct calendar years a species must have data in (for a given response
variable) to be retained.Acts alongside \code{min_records}; a species must
satisfy \strong{both} criteria.}

\item{alpha_ref}{Numeric scalar in (0, 1) (default \code{0.05}).Baseline
significance level used for all hypothesis tests and confidence-interval
calculations.This value is also the starting point for the
sample-size correction applied when \code{use_Ncorrected_alpha = TRUE}.}

\item{use_Ncorrected_alpha}{Logical (default \code{TRUE}).When \code{TRUE}, the
effective significance threshold for any individual species with more
than 100 records is reduced according to
\deqn{\alpha_{\mathrm{eff}} = \alpha_{\mathrm{ref}} \times \sqrt{100 / N}}{
alpha_eff = alpha_ref * sqrt(100 / N)}
where \eqn{N} is the number of records for that species and response
variable.This penalises the inflated statistical power that comes with
large sample sizes, requiring a larger observed effect before a species is
classified as significant.When \code{FALSE}, \code{alpha_ref} is applied uniformly
to all species.}

\item{individual_lm_min_thresholds}{Named numeric vector or \code{NULL}
(default \code{c(Temperature = 0.01, Elevation = 50)}).Minimum ecological
effect size that must be exceeded before a species can be assigned a
non-conformance class (TT, TA, SA, or SD).Names must be a subset of
\code{c("Temperature", "Elevation")} and all values must be positive.The
quantity being thresholded depends on \code{individual_lm_threshold_type}:
\describe{
\item{\code{"speciesslope"}}{ (default) The absolute value of the species-specific
temporal slope must exceed the threshold (e.g. |slope| > 0.01
deg C yr\eqn{^{-1}} for Temperature). Evaluates how much the species is shifting
in absolute terms.}
\item{\code{"slopediff"}}{The absolute value of the difference between the
species slope and the global slope must exceed the threshold.
Evaluates how much the species is shifting relative to the average
community trend. }
}
If \code{NULL}, no ecological threshold is applied and classification relies
entirely on statistical significance.Must be \code{NULL} jointly with
\code{individual_lm_threshold_type}, or non-\code{NULL} jointly; mixed states
produce an error.}

\item{individual_lm_threshold_type}{Character scalar (default
\code{"speciesslope"}).Determines which quantity is compared against
\code{individual_lm_min_thresholds}.Must be one of:
\describe{
\item{\code{"speciesslope"}}{Threshold is applied to the absolute
species-specific slope.}
\item{\code{"slopediff"}}{Threshold is applied to the absolute difference
between the species slope and the global slope.}
}}

\item{verbose}{Logical (default \code{TRUE}).When \code{TRUE}, the function prints
progress messages to the console via \code{message()}, including the number of
species retained or excluded at the filtering step for each response
variable.Set to \code{FALSE} to suppress all messages (useful inside loops
or parallel computations).}
}
\value{
A named list with the following elements:

\describe{

\item{\code{params}}{Named list storing the values of all function arguments
(except \code{data} and \code{spp}) as supplied by the caller.Useful for
reproducibility and logging.}

\item{\code{species_filter}}{Data frame with one row per species x response
combination, summarising the filtering step.Columns:
\describe{
\item{\code{Species}}{Character.Species name.}
\item{\code{response}}{Character.Response variable (\code{"Temperature"} or
\code{"Elevation"}).}
\item{\code{n_records}}{Integer.Total number of non-missing records for
this species and response.}
\item{\code{n_years}}{Integer.Number of distinct calendar years with at
least one non-missing record.}
\item{\code{retained}}{Logical.\code{TRUE} if the species meets both
\code{min_records} and \code{min_years} thresholds and was therefore
included in the modelling step.}
}
}

\item{\code{environmental_global_lm}}{Data frame with one row per response
variable containing statistics from the pooled (community-level) OLS
regression.Columns:
\describe{
\item{\code{response}}{Character.Response variable.}
\item{\code{model}}{Character.Always \code{"global_lm"}.}
\item{\code{global_lm_slope}}{Numeric.Estimated temporal slope of the
global model (units per year: deg C yr\eqn{^{-1}} or m yr\eqn{^{-1}}).}
\item{\code{global_lm_se}}{Numeric.Standard error of the global slope.}
\item{\code{global_lm_t}}{Numeric.t-statistic for the global slope
(H0: slope = 0).}
\item{\code{global_lm_p_value}}{Numeric.Two-tailed p-value for the
global slope test.}
\item{\code{global_lm_conf_low}}{Numeric.Lower bound of the 95 \\%
confidence interval for the global slope.}
\item{\code{global_lm_conf_high}}{Numeric.Upper bound of the 95 \\%
confidence interval for the global slope.}
\item{\code{n_records}}{Integer.Total number of records used to fit the
global model (all valid records across retained species).}
\item{\code{n_years}}{Integer.Number of distinct calendar years
represented in the global model dataset.}
}
}

\item{\code{environmental_individual_lm}}{Data frame with one row per species
x response combination for all species that passed the filtering step.
Contains species-level OLS statistics plus comparisons against the
global model.Columns:
\describe{
\item{\code{Species}}{Character.Species name.}
\item{\code{response}}{Character.Response variable.}
\item{\code{n_records}}{Integer.Number of records used to fit this
species model.}
\item{\code{n_years}}{Integer.Number of distinct years in the species
data for this response.}
\item{\code{individual_slope}}{Numeric.Estimated temporal slope for this
species (deg C yr\eqn{^{-1}} or m yr\eqn{^{-1}}).}
\item{\code{individual_slope_se}}{Numeric.Standard error of the
species-specific slope.}
\item{\code{individual_t}}{Numeric.t-statistic for the species slope
(H0: slope = 0).}
\item{\code{individual_p_value}}{Numeric.Two-tailed p-value for the
species slope test (raw, before FDR correction).}
\item{\code{individual_conf_low}}{Numeric.Lower bound of the 95 \\%
confidence interval for the species slope.}
\item{\code{individual_conf_high}}{Numeric.Upper bound of the 95 \\%
confidence interval for the species slope.}
\item{\code{global_lm_slope}}{Numeric.Global slope for this response
variable (copied from \code{environmental_global_lm} for
convenience).}
\item{\code{slope_diff_signed}}{Numeric.Signed difference between the
species slope and the global slope
(\code{individual_slope - global_lm_slope}).Positive values
indicate the species trend is steeper than the community
average.}
\item{\code{slope_diff_direction}}{Character.Direction of
\code{slope_diff_signed}: \code{"positive"}, \code{"negative"}, or \code{"zero"}.}
\item{\code{slope_diff}}{Numeric.Absolute value of \code{slope_diff_signed}.}
\item{\code{slope_diff_t}}{Numeric.Welch-Satterthwaite t-statistic for
the test of equality between the species slope and the global
slope.}
\item{\code{slope_diff_df}}{Numeric.Welch-Satterthwaite degrees of
freedom for \code{slope_diff_t}.}
\item{\code{slope_diff_p_value}}{Numeric.Two-tailed p-value for the
slope-difference test (raw, before FDR correction).}
\item{\code{individual_direction}}{Character.Direction of the species
slope itself: \code{"positive"}, \code{"negative"}, or \code{"zero"}.}
\item{\code{individual_p_adj_fdr}}{Numeric.FDR-adjusted (BH method)
p-value for the species slope test, computed within each
response variable separately.}
\item{\code{slope_diff_p_adj_fdr}}{Numeric.FDR-adjusted (BH method)
p-value for the slope-difference test.}
\item{\code{alpha}}{Numeric.Effective significance threshold applied to
this species.Equals \code{alpha_ref} for species with <= 100
records, or the sample-size-corrected value when
\code{use_Ncorrected_alpha = TRUE} and N > 100.}
\item{\code{individual_significant}}{Logical.\code{TRUE} if
\code{individual_p_adj_fdr < alpha} for this species.}
\item{\code{slope_diff_significant}}{Logical.\code{TRUE} if
\code{slope_diff_p_adj_fdr < alpha} for this species.}
}
}

\item{\code{environmental_comparison}}{Data frame extending
\code{environmental_individual_lm} with the classification columns added by
\code{classify_lm_individual_slope()}.Contains all the columns described
for \code{environmental_individual_lm} plus:
\describe{
\item{\code{individual_slope_above_threshold}}{Logical.\code{TRUE} if the
relevant quantity (species slope or slope difference, depending
on \code{individual_lm_threshold_type}) exceeds the minimum
ecological threshold defined in \code{individual_lm_min_thresholds}.
Always \code{TRUE} when no threshold is specified.Retained for
downstream quality audits.}
\item{\code{individual_response_class}}{Character.Final environmental
response classification for this species and response variable.
Possible values: \code{"TT"} (Thermal Tolerance), \code{"TA"} (Thermal
Adjustment), \code{"TC"} (Thermal Conformance) for Temperature;
\code{"SA"} (Spatial Adaptation), \code{"SD"} (Spatial Discordance),
\code{"SC"} (Spatial Conformance) for Elevation.A non-conformance
class (TT, TA, SA, SD) is assigned only when both
\code{individual_significant} and \code{slope_diff_significant} are
\code{TRUE} and \code{individual_slope_above_threshold} is \code{TRUE}.
Otherwise the species defaults to the conformance class (TC or
SC).}
}
}

}
}
\description{
\code{spp_trend_environmental()} models temporal trends in environmental
variables - Temperature and/or Elevation - for each species in a community
dataset and evaluates whether species-specific shifts differ from the
pooled community trend.

For every requested response variable the function fits:
\enumerate{
\item A \strong{global ordinary least-squares (OLS) regression} pooled across
all valid species, capturing the average community-level temporal
trend.
\item An \strong{independent OLS regression per species}, yielding
species-specific temporal slopes together with their standard errors,
t-statistics, and 95 \\% confidence intervals.
}

Species-specific slopes are then compared against the global slope using
\strong{Welch-Satterthwaite t-tests}. Raw p-values from both the individual
species regressions and the slope-difference tests are corrected for
multiple testing with the \strong{Benjamini-Hochberg False Discovery Rate (FDR)}
procedure, applied separately for each response variable. An optional
sample-size correction penalises the significance threshold for
well-sampled species (N > 100).
}
\section{Classification system}{

Each species is assigned an \strong{environmental response class} reflecting the
direction and significance of its temporal trend (or its deviation from the
global trend, depending on \code{individual_lm_threshold_type}):

\describe{
\item{\code{"TT"} - Thermal Tolerance}{Temperature response.The species is
moving toward warmer environments over time: its individual slope (or
its deviation from the global slope, under \code{"slopediff"}) is
positive, statistically significant, and exceeds the minimum
ecological threshold.}
\item{\code{"TA"} - Thermal Adjustment}{Temperature response.The species is
shifting toward cooler environments: slope (or deviation) is
negative, significant, and exceeds the threshold.}
\item{\code{"TC"} - Thermal Conformance}{Temperature response.Default class
assigned when the individual trend is non-significant or not
distinguishable from the global trend.}
\item{\code{"SA"} - Spatial Adaptation}{Elevation response.The species is
tracking higher elevations over time: slope (or deviation) is
positive, significant, and exceeds the threshold.}
\item{\code{"SD"} - Spatial Discordance}{Elevation response.The species is
shifting to lower elevations: slope (or deviation) is negative,
significant, and exceeds the threshold.}
\item{\code{"SC"} - Spatial Conformance}{Elevation response.Default class
when the elevation trend is non-significant or indistinguishable from
the global trend.}
}
}

\section{Time variable}{

Internally the function constructs a continuous, mean-centred time
predictor (\code{time_cont_c}) from \code{Year} and \code{Month} as
\code{Year + (Month - 1) / 12 - mean(...)}. Slopes are therefore expressed in
\strong{environmental units per year} (deg C yr\eqn{^{-1}} for Temperature, m yr\eqn{^{-1}} for
Elevation).
}

\examples{
# Minimal example with simulated data
set.seed(42)
n <- 600
sim_data <- data.frame(
Species = sample(paste0("Sp", 1:5), n, replace = TRUE),
Year = sample(2000:2020, n, replace = TRUE),
Month = sample(1:12, n, replace = TRUE),
Temperature = rnorm(n, mean = 15, sd = 3),
Elevation = rnorm(n, mean = 500, sd = 100))

results <- spp_trend_environmental(
data = sim_data,
responses = c("Temperature", "Elevation"),
min_records = 20,
min_years = 5,
alpha_ref = 0.05,
use_Ncorrected_alpha = FALSE,
individual_lm_min_thresholds = c(Temperature = 0.01, Elevation = 50),
individual_lm_threshold_type = "speciesslope",
verbose = FALSE
)

# Inspect the classification table
head(results$environmental_comparison)

# Species retained per response
results$species_filter[results$species_filter$retained, ]

# Global community trends
results$environmental_global_lm

}
